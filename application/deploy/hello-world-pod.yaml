apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  restartPolicy: OnFailure
  containers:
    - name: hello-world
      image: busybox
      command:
        - sh
        - -c
        - |
          echo "Hello World from AKS"
          sleep 3600
